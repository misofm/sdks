{
  "name": "@misofm/sdk-monorepo",
  "private": true,
  "packageManager": "bun@1.4.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test",
    "build": "bun run --filter @misofm/utils build && bun run --filter @misofm/streaming build && bun run --filter @misofm/protocol build && bun run --filter @misofm/platform build && bun run --filter @misofm/transcoding build",
    "test:consumer": "bun scripts/check-consumer.ts",
    "codegen": "bun scripts/codegen.ts",
    "codegen:check": "bun run codegen && git diff --exit-code -- packages/protocol/src/contracts"
  },
  "devDependencies": {
    "@mysten/codegen": "^0.12.6",
    "@mysten/sui": "2.29.0",
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
